# Diamond Club Archives

Diamond Club Archives is search, filtering and tracking for videos related to Diamond Club

## Installation

* Install packages
	* `composer install`
	* `npm install`
* Copy `.env.example` to `.env`
* Set your configuration in `.env`
